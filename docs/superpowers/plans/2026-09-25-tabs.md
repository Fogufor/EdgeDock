# Вкладки в панели — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** модули открываются в панели по одному: слева колонка значков-вкладок, справа блок выбранной вкладки; панель становится ниже.

**Architecture:** `PanelWindow` получает колонку вкладок (`ItemsControl` из `PanelTab`) и хост, в котором виден один блок; `DockWindow` решает, какая вкладка открыта, будит и усыпляет модули (`OnExpanded`/`OnCollapsed` = вкладка показана/скрыта), запоминает вкладку в `state.json` и меняет высоту панели, не двигая её верхний край (`PlacementService.KeepTop`). Модуль сам сообщает подпись и значок вкладки (`IDockModule.Title`, `TabGlyph`).

**Tech Stack:** C#, .NET 10, WPF, WPF UI 4.3. Проверка — сквозной тест через UI Automation (PowerShell 5.1).

**Spec:** `SPEC.md` → «Правила производительности» (сон скрытой вкладки), «Поведение дока» → «Вкладки», «Архитектура» (`IDockModule`), «Настройки» (`modules`, `dock.tab`).

## Global Constraints

* Визуальные значения — только из `Themes/Tokens.xaml`; никаких `Effect`; анимации только `Opacity`/`RenderTransform`.
* Всё в окнах дока — `Focusable="False"`.
* Скрытая вкладка = сон (как свёрнутая панель); интерфейс модуля — при первом открытии вкладки.
* Новых зависимостей нет. Код и комментарии — на русском.
* Коммит после этапа; сообщение заканчивается строкой `Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>`.

## Файлы

* Создать `EdgeDock/PanelTab.cs` — значок вкладки (id, подпись, глиф, открыта ли).
* Изменить `EdgeDock/PanelWindow.xaml(.cs)` — колонка вкладок, хост одного блока, события `TabClicked`, `FileDragEntered`.
* Изменить `EdgeDock/DockWindow.xaml.cs` — выбор вкладки, жизненный цикл модулей, запоминание, высота с неподвижным верхом.
* Изменить `EdgeDock/Core/IDockModule.cs` и четыре модуля — `Title`, `TabGlyph`.
* Изменить `EdgeDock/Core/PlacementService.cs` — `DockState.Tab`, `KeepTop`.
* Изменить `EdgeDock/Core/SettingsService.cs` — порядок модулей по умолчанию.
* Изменить `EdgeDock/Themes/Tokens.xaml`, `README.md`, `EdgeDock/EdgeDock.csproj` (1.5.0).
* Тесты (scratchpad): новый `tabstest.ps1`; `pinstest.ps1` — открывать вкладку «Закреплённое», сохранять и возвращать `state.json`.
* У пользователя: в `settings.json` → `modules` поменять местами `audio` и `pins` (согласовано).

---

### Task 1: Тест вкладок (падает до реализации)

**Files:** Create `<scratchpad>/tabstest.ps1`; Modify `<scratchpad>/pinstest.ps1`; временный хук `EDGEDOCK_TEST_EXPAND` в `DockWindow` (строка `// EXPERIMENT`, не коммитится).

**Interfaces — Produces:** UIA-идентификаторы: значки вкладок `Tab.<id>` (Name = подпись), блоки `View.<id>`.

- [ ] **Step 1:** хук разворота — как в плане «Закреплённого» (`EDGEDOCK_TEST_EXPAND=1` → `Expand()` раз в секунду).
- [ ] **Step 2:** `settings.json` пользователя: в `modules` поменять местами `"audio"` и `"pins"` (одно поле, остальное не трогать).
- [ ] **Step 3:** `tabstest.ps1` — помощники из `pinstest.ps1` (до `AddPin`) плюс:

```powershell
$state = Join-Path $env:LOCALAPPDATA 'EdgeDock\state.json'
$stateBackup = Join-Path $PSScriptRoot 'state.backup.json'
function TabIds { @(FindAllId 'Tab.pocket') + @(FindAllId 'Tab.media') + @(FindAllId 'Tab.pins') + @(FindAllId 'Tab.audio') | Sort-Object { $_.Current.BoundingRectangle.Top } | ForEach-Object { $_.Current.AutomationId } }
function ShownViews { foreach ($id in 'pocket', 'media', 'pins', 'audio') { if (FindId "View.$id") { $id } } }
function PanelOf($e) { $walker = [System.Windows.Automation.TreeWalker]::ControlViewWalker; $top = $e; while ($top.Current.NativeWindowHandle -eq 0) { $top = $walker.GetParent($top) }; $top.Current.BoundingRectangle }
function StartTabs { $env:EDGEDOCK_TEST_EXPAND = '1'; $script:proc = Start-Process $Exe -PassThru; Remove-Item Env:EDGEDOCK_TEST_EXPAND; [void](WaitFor { FindId 'Tab.pocket' } 15000) }

# pins.json пользователя — сохранить копию, в конце вернуть как было.
$pinsBackup = Join-Path $PSScriptRoot 'pins.backup.json'
$hadPins = Test-Path $pins
if ($hadPins) { Copy-Item $pins $pinsBackup -Force }
try {
    StopWidget
    Copy-Item $state $stateBackup -Force
    $seed = (1..16 | ForEach-Object { '{"label":"Длинное название ' + $_ + '","value":"#tag' + $_ + '"}' }) -join ','
    [IO.File]::WriteAllText($pins, "[$seed]", (New-Object Text.UTF8Encoding $false))
    StartTabs
    if (-not (FindId 'Tab.pocket')) { Check 'tabs present' $false; throw 'no tabs' }
    Check 'tabs top to bottom' (((TabIds) -join ',') -eq 'Tab.pocket,Tab.media,Tab.pins,Tab.audio') ((TabIds) -join ',')
    $tops = @(); $heights = @{}
    foreach ($id in 'pocket', 'media', 'pins', 'audio') {
        Invoke (FindId "Tab.$id")
        $view = WaitFor { FindId "View.$id" } 5000
        Start-Sleep -Milliseconds 400
        $shown = (ShownViews) -join ','
        Check "tab $id shows only its block" ($shown -eq $id) $shown
        $panel = PanelOf $view
        # Высота — по большему из двух: колонка значков (низ последнего значка + 4 + 8) или блок (его низ + 8).
        # Обрезанный блок UIA показывает только видимой частью — тогда ожидаемый низ окажется ниже панели и проверка упадёт.
        $railBottom = (FindId 'Tab.audio').Current.BoundingRectangle.Bottom + 4 + 8
        $expected = [Math]::Max($railBottom, $view.Current.BoundingRectangle.Bottom + 8)
        Check "tab $id height = max(rail, block)" ([Math]::Abs($panel.Bottom - $expected) -le 1) "panel bottom $($panel.Bottom), expected $expected"
        $tops += $panel.Top; $heights[$id] = $panel.Height
    }
    Check 'top edge stays' (@($tops | Select-Object -Unique).Count -eq 1) ($tops -join ',')
    # «Закреплённое» с 16 длинными плашками выше колонки значков: панель растёт на нём и возвращается на «Кармане».
    Check 'tall tab makes panel taller' ($heights['pins'] -gt $heights['pocket']) "pins=$($heights['pins']) pocket=$($heights['pocket'])"
    Invoke (FindId 'Tab.pocket'); [void](WaitFor { FindId 'View.pocket' }); Start-Sleep -Milliseconds 400
    $back = (PanelOf (FindId 'View.pocket')).Height
    Check 'panel shrinks back' ($back -eq $heights['pocket']) "pocket again=$back"
    Invoke (FindId 'Tab.audio'); [void](WaitFor { FindId 'View.audio' }); Start-Sleep -Milliseconds 500
    StopWidget
    Check 'tab saved' ((Get-Content $state -Raw -Encoding UTF8 | ConvertFrom-Json).dock.tab -eq 'audio')
    StartTabs; [void](WaitFor { FindId 'View.audio' } 5000)
    Check 'last tab after restart' (((ShownViews) -join ',') -eq 'audio') ((ShownViews) -join ',')
}
catch { Check "error: $_" $false }
finally {
    StopWidget
    Remove-Item $pins, "$pins.tmp" -ErrorAction SilentlyContinue
    if ($hadPins) { Copy-Item $pinsBackup $pins -Force; Remove-Item $pinsBackup }
    if (Test-Path $stateBackup) { Copy-Item $stateBackup $state -Force; Remove-Item $stateBackup }
    if (Test-Path $installed) { Start-Process $installed }
}
$results
'{0} passed, {1} failed' -f @($results | Where-Object { $_ -like 'PASS*' }).Count, @($results | Where-Object { $_ -like 'FAIL*' }).Count
```

- [ ] **Step 4:** `pinstest.ps1`: `StartTest` после запуска ждёт `Tab.pins`, нажимает его и ждёт `PinsAddButton`; после первого `StopWidget` — копия `state.json`, в `finally` после `StopWidget` — вернуть.
- [ ] **Step 5:** сборка, `tabstest.ps1` — ожидается `FAIL tabs present`.

Примечание по ходу работы: блоки обычно ниже колонки значков (168), и тогда все вкладки дают одну высоту — это по дизайну. Поэтому тест засевает в «Закреплённое» 16 длинных плашек (своё `pins.json` пользователя сохраняет и возвращает) и сверяет высоту как «max(колонка, блок)».

---

### Task 2: Модули и данные

**Files:** `Core/IDockModule.cs`, `Modules/*/…Module.cs`, `Core/PlacementService.cs`, `Core/SettingsService.cs`, `Themes/Tokens.xaml`.

**Interfaces — Produces:** `IDockModule.Title`, `IDockModule.TabGlyph` (ключ в Tokens); `DockState.Tab`; `PlacementService.KeepTop(PixelRect wanted, int top, MonitorInfo mon)`; ресурсы `Tab.Size`, `Tab.RailWidth`, `Margin.TabRail`, `Margin.Tab`, `Motion.TabSwitch`, `Glyph.Pictures`, `Glyph.Pin`.

- [ ] **Step 1: `IDockModule`** — после `Id`:

```csharp
    /// <summary>Подпись вкладки (всплывающая подсказка у значка).</summary>
    string Title { get; }

    /// <summary>Ключ значка вкладки в Tokens.xaml (Glyph.*).</summary>
    string TabGlyph { get; }
```

и описания: `OnExpanded` — «Вкладка модуля показана (панель развернулась на ней или на неё переключились).», `OnCollapsed` — «Вкладка скрыта или панель свернулась: освободить превью, остановить анимации.».

- [ ] **Step 2: модули** — `Pocket`: `Title => "Карман"`, `TabGlyph => "Glyph.Pictures"`; `Media`: `"Музыка"`, `"Glyph.Music"`; `Pins`: `"Закреплённое"`, `"Glyph.Pin"`; `Audio`: `"Звук"`, `"Glyph.Speaker"`.

- [ ] **Step 3: `DockState.Tab`**

```csharp
    /// <summary>Id модуля последней открытой вкладки панели.</summary>
    public string? Tab { get; set; }
```

и в `PlacementService`:

```csharp
    /// <summary>
    /// Открытая панель меняет высоту (другая вкладка, выросло содержимое): верхний край остаётся на месте,
    /// чтобы значки вкладок не уезжали из-под мыши. Не помещается — поднять ровно настолько, чтобы не выйти за рабочую область.
    /// </summary>
    public PixelRect KeepTop(PixelRect wanted, int top, MonitorInfo mon)
    {
        var work = mon.Work;
        int gap = Px(_panelGap, mon);
        int highest = work.Top + gap, lowest = Math.Max(highest, work.Bottom - gap - wanted.Height);
        return wanted with { Top = Math.Clamp(top, highest, lowest) };
    }
```

- [ ] **Step 4: порядок по умолчанию** — `Modules = ["meeting", "pocket", "media", "pins", "audio"]`.

- [ ] **Step 5: токены**

```xml
    <!-- Вкладки: колонка значков слева. Ширина колонки = отступ слева (8) + значок (36). -->
    <sys:Double x:Key="Tab.Size">36</sys:Double>
    <sys:Double x:Key="Tab.RailWidth">44</sys:Double>
    <Thickness x:Key="Margin.TabRail">0,0,0,8</Thickness>
    <Thickness x:Key="Margin.Tab">0,0,0,4</Thickness>
```

`<Duration x:Key="Motion.TabSwitch">0:0:0.12</Duration>` — в «Движение»; глифы `Glyph.Pictures` = `&#xE91B;`, `Glyph.Pin` = `&#xE718;`.

---

### Task 3: Панель и док

**Files:** Create `EdgeDock/PanelTab.cs`; Modify `PanelWindow.xaml`, `PanelWindow.xaml.cs`, `DockWindow.xaml.cs`.

- [ ] **Step 1: `PanelTab.cs`**

```csharp
using System.ComponentModel;

namespace EdgeDock;

/// <summary>Значок вкладки в колонке слева: модуль, подпись (подсказка), глиф и отметка «открыта».</summary>
public sealed class PanelTab : INotifyPropertyChanged
{
    private bool _isActive;

    public PanelTab(string id, string title, string glyph)
    {
        Id = id;
        Title = title;
        Glyph = glyph;
    }

    public string Id { get; }
    public string Title { get; }
    public string Glyph { get; }

    /// <summary>Для UI Automation: «Tab.pins».</summary>
    public string AutomationId => "Tab." + Id;

    /// <summary>Вкладка открыта: заливка и акцентная черта слева.</summary>
    public bool IsActive
    {
        get => _isActive;
        set
        {
            if (_isActive == value) return;
            _isActive = value;
            PropertyChanged?.Invoke(this, new(nameof(IsActive)));
        }
    }

    public event PropertyChangedEventHandler? PropertyChanged;
}
```

- [ ] **Step 2: `PanelWindow.xaml`** — `xmlns:local="clr-namespace:EdgeDock"`; в `Window.Resources` стиль значка:

```xml
    <Window.Resources>
        <!-- Значок вкладки — как пункт навигации Windows 11: заливка при наведении, у открытой ещё и акцентная черта слева. -->
        <Style x:Key="TabButton" TargetType="Button">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="Width" Value="{StaticResource Tab.Size}" />
            <Setter Property="Height" Value="{StaticResource Tab.Size}" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Background="Transparent">
                            <Border x:Name="Fill" CornerRadius="{StaticResource Radius.Small}" />
                            <Border x:Name="Indicator" HorizontalAlignment="Left" Visibility="Collapsed"
                                    Width="{StaticResource Indicator.Width}" Height="{StaticResource Indicator.Height}"
                                    CornerRadius="{StaticResource Radius.Strip}" Background="{DynamicResource Brush.Accent}" />
                            <TextBlock x:Name="Glyph" Text="{Binding Glyph}" FontFamily="{StaticResource Font.Icons}"
                                       FontSize="{StaticResource FontSize.IconLarge}" Foreground="{DynamicResource Brush.Text.Secondary}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Fill" Property="Background" Value="{DynamicResource Brush.Subtle.Hover}" />
                                <Setter TargetName="Glyph" Property="Foreground" Value="{DynamicResource Brush.Text.Primary}" />
                            </Trigger>
                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                <Setter TargetName="Fill" Property="Background" Value="{DynamicResource Brush.Subtle.Hover}" />
                                <Setter TargetName="Indicator" Property="Visibility" Value="Visible" />
                                <Setter TargetName="Glyph" Property="Foreground" Value="{DynamicResource Brush.Text.Primary}" />
                            </DataTrigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Fill" Property="Background" Value="{DynamicResource Brush.Subtle.Pressed}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
```

`Body` — две колонки (`Auto`, `*`): `Header` с `Grid.ColumnSpan="2"`; во второй строке колонка значков и хост:

```xml
            <!-- Вкладки: по значку на модуль, сверху вниз. -->
            <ItemsControl x:Name="Tabs" Grid.Row="1" Width="{StaticResource Tab.RailWidth}"
                          Margin="{StaticResource Margin.TabRail}" VerticalAlignment="Top" Focusable="False">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type local:PanelTab}">
                        <Button Style="{StaticResource TabButton}" Margin="{StaticResource Margin.Tab}" ToolTip="{Binding Title}"
                                AutomationProperties.Name="{Binding Title}" AutomationProperties.AutomationId="{Binding AutomationId}"
                                Click="OnTabClick" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Блок открытой вкладки; остальные созданные блоки здесь же, но скрыты. -->
            <Grid x:Name="ModuleHost" Grid.Row="1" Grid.Column="1" Margin="{StaticResource Inset.PanelBody}" />
```

`EmptyText` — `Grid.Row="1" Grid.Column="1"`.

- [ ] **Step 3: `PanelWindow.xaml.cs`** — вместо `ModuleViews`/`AddModuleView`:

```csharp
    /// <summary>Нажали значок вкладки (id модуля).</summary>
    public event Action<string>? TabClicked;

    /// <summary>Над панелью тащат файл или картинку — DockWindow открывает «Карман».</summary>
    public event Action? FileDragEntered;

    /// <summary>Значки вкладок слева, в порядке модулей.</summary>
    public void SetTabs(IReadOnlyList<PanelTab> tabs) => Tabs.ItemsSource = tabs;

    /// <summary>Показать блок вкладки, остальные скрыть. Новый блок добавляется при первом показе. fade — содержимое проявляется.</summary>
    public void ShowView(FrameworkElement view, bool fade)
    {
        if (!ModuleHost.Children.Contains(view)) ModuleHost.Children.Add(view);
        foreach (UIElement child in ModuleHost.Children)
            child.Visibility = child == view ? Visibility.Visible : Visibility.Collapsed;
        if (fade)
            Animate(view, OpacityProperty, 0, 1, (Duration)FindResource("Motion.TabSwitch"), new CubicEase { EasingMode = EasingMode.EaseOut });
    }

    /// <summary>Убрать все блоки (модули пересоздаются по «Перезагрузить настройки»).</summary>
    public void ClearViews() => ModuleHost.Children.Clear();

    private void OnTabClick(object sender, RoutedEventArgs e)
    {
        if (((FrameworkElement)sender).DataContext is PanelTab tab) TabClicked?.Invoke(tab.Id);
    }
```

и `OnPreviewDragEnter`:

```csharp
    protected override void OnPreviewDragEnter(DragEventArgs e)
    {
        PointerEntered?.Invoke();
        FileDragEntered?.Invoke();
    }
```

- [ ] **Step 4: `DockWindow.xaml.cs`** — поля вместо `_viewsCreated`:

```csharp
    private readonly Dictionary<IDockModule, FrameworkElement> _views = [];
    private List<PanelTab> _tabs = [];
    private IDockModule? _active;       // модуль открытой вкладки
    private PixelRect _panelRect;       // где панель сейчас (в том числе пока Show() ещё не сделал её видимой)
```

`BuildModules` — `_views.Clear(); _active = null; _panel?.ClearViews();` вместо старых строк, в конце:

```csharp
        _tabs = _modules.Select(m => new PanelTab(m.Id, m.Title, (string)FindResource(m.TabGlyph))).ToList();
        _panel?.SetTabs(_tabs);
```

`OnDragEnter` — `Expand("pocket"); ShowTab("pocket");` вместо `Expand();` (комментарий: «…и сразу „Карман“ — файлы принимает только полка»).

`Expand`:

```csharp
    /// <param name="tabId">Какую вкладку открыть; null — последнюю открытую (или первую).</param>
    private void Expand(string? tabId = null)
    {
        if (_expanded || _suspended || _dragging || _monitor == null) return;
        _expandTimer.Stop();

        var panel = EnsurePanel();
        _expanded = true;
        _active = _modules.FirstOrDefault(m => m.Id == (tabId ?? _settings.State.Dock.Tab)) ?? _modules.FirstOrDefault();
        if (_active != null)
        {
            panel.ShowView(ViewOf(_active), fade: false);
            _active.OnExpanded();
        }
        MarkActiveTab();

        var (rect, slideX, slideY) = PanelPlacement();
        _panelRect = rect;
        panel.ShowAt(rect, slideX, slideY);
        UpdateVisual();
    }
```

новые методы рядом:

```csharp
    /// <summary>Интерфейс модуля: создаётся при первом открытии его вкладки.</summary>
    private FrameworkElement ViewOf(IDockModule module)
    {
        if (_views.TryGetValue(module, out var view)) return view;
        view = module.CreateView();
        AutomationProperties.SetAutomationId(view, "View." + module.Id);
        _views[module] = view;
        return view;
    }

    /// <summary>Открыть вкладку в развёрнутой панели: прежний модуль засыпает, новый просыпается, высота — по новому блоку.</summary>
    private void ShowTab(string id)
    {
        var module = _modules.FirstOrDefault(m => m.Id == id);
        if (!_expanded || module == null || module == _active) return;
        _active?.OnCollapsed();
        _active = module;
        _panel!.ShowView(ViewOf(module), fade: true);
        module.OnExpanded();
        MarkActiveTab();
        ResizePanel();
    }

    /// <summary>Отметить открытую вкладку и запомнить её в state.json (только если сменилась).</summary>
    private void MarkActiveTab()
    {
        foreach (var tab in _tabs) tab.IsActive = tab.Id == _active?.Id;
        if (_active == null || _settings.State.Dock.Tab == _active.Id) return;
        _settings.State.Dock.Tab = _active.Id;
        _settings.SaveState();
    }

    /// <summary>Открытая панель меняет высоту (другая вкладка, выросло содержимое): верхний край на месте.</summary>
    private void ResizePanel()
    {
        if (!_expanded || _dragging || _monitor == null || _panel == null) return;
        _panelRect = _placement.KeepTop(PanelPlacement().Rect, _panelRect.Top, _monitor);
        _panel.MoveTo(_panelRect);
    }
```

`PanelPlacement` — ширина `_settings.Settings.Dock.Width + (double)FindResource("Tab.RailWidth")`.

`EnsurePanel` — после создания: `_panel.SetTabs(_tabs); _panel.TabClicked += ShowTab; _panel.FileDragEntered += () => ShowTab("pocket");`; `ContentResized += ResizePanel` вместо прежнего обработчика.

`NotifyCollapsed` — `_active?.OnCollapsed();` (спит только открытая вкладка — остальные и так не просыпались).

`using System.Windows.Automation;`.

- [ ] **Step 5:** сборка без ошибок и предупреждений; `tabstest.ps1` и `pinstest.ps1` — всё `PASS` (правый клик в `pinstest` может быть `SKIP`, если настоящий курсор не над панелью).
- [ ] **Step 6:** снимки панели на двух вкладках (короткая и длинная) — проверить вид глазами в светлой теме.
- [ ] **Step 7:** убрать `// EXPERIMENT`, коммит.

---

### Task 4: Выпуск

- [ ] README: в «Док» — «Блоки открываются вкладками: значки слева — Карман, Музыка, Закреплённое, Звук; панель помнит последнюю».
- [ ] Версия 1.5.0, коммит, `git push`, тег `v1.5.0`, `dotnet publish -r win-x64 -o artifacts`, установка через `installreal.ps1`, проверка: 1.5.0 запущен, лог пуст, релиз на GitHub зелёный.

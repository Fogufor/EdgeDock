# Блок «Закреплённое» — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** блок панели с плашками «значок + название»; клик копирует скрытое под названием значение (ссылку, тег, телефон, текст); добавление и правка — в отдельном маленьком окне.

**Architecture:** новый модуль `pins` по образцу существующих (`IDockModule`, ленивый `CreateView`). Данные — `pins.json` через `PinStore` (запись через временный файл; нечитаемый файл не перезаписывается). Окно правки — обычное активируемое окно WPF с фоном Mica (общий помощник `WindowBackdrop`, вынесенный из окна установки), потому что панель фокус не берёт.

**Tech Stack:** C#, .NET 10, WPF, WPF UI 4.3 (`ui:Button`), System.Text.Json. Проверка — сквозной тест через UI Automation (PowerShell 5.1), как и раньше в проекте.

**Spec:** `SPEC.md` → «Настройки» (`pins.json`), «Закреплённое», «Этапы» п. 5.

## Global Constraints

* Любое визуальное значение — только из `Themes/Tokens.xaml`; никаких `Effect` WPF; никаких счётчиков на экране.
* Всё в окнах дока — `Focusable="False"`, иначе клик активирует окно. Окно правки — обычное, фокус берёт.
* Никакой работы в фоне: единственный таймер — одноразовый на 1,5 с подтверждения.
* Новых зависимостей не добавлять.
* Код и комментарии — на русском, понятные, без лишних абстракций.
* `pins.json` пишет только виджет; `settings.json` виджет не трогает.
* Коммит после этапа; сообщение заканчивается строкой `Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>`.

## Файлы

* Создать `EdgeDock/Core/WindowBackdrop.cs` — фон Mica для обычных окон (вынесен из `SetupWindow`).
* Создать `EdgeDock/Modules/Pins/PinKind.cs` — вид значения (ссылка / тег / телефон / текст).
* Создать `EdgeDock/Modules/Pins/PinStore.cs` — чтение и запись `pins.json`.
* Создать `EdgeDock/Modules/Pins/PinItem.cs` — плашка для привязки.
* Создать `EdgeDock/Modules/Pins/PinsModule.cs` — модуль: список, копирование, окно правки.
* Создать `EdgeDock/Modules/Pins/PinsView.xaml(.cs)` — блок в панели.
* Создать `EdgeDock/Modules/Pins/PinEditorWindow.xaml(.cs)` — окно «Новый элемент» / «Изменить».
* Изменить `EdgeDock/Core/AppPaths.cs`, `EdgeDock/Themes/Tokens.xaml`, `EdgeDock/DockWindow.xaml.cs`, `EdgeDock/Setup/SetupWindow.xaml.cs`, `README.md`, `IDEAS.md`, `EdgeDock/EdgeDock.csproj`.
* Тест (вне репозитория, в scratchpad): `pinstest.ps1`.

---

### Task 1: Сквозной тест (падает до реализации)

**Files:**
- Create: `<scratchpad>/pinstest.ps1` (UTF-8 с BOM — в нём кириллица)
- Modify (временно, не коммитится): `EdgeDock/DockWindow.xaml.cs` — строка `// EXPERIMENT` в конструкторе

**Interfaces:**
- Produces: UIA-идентификаторы, которые обязана выставить реализация: `PinsAddButton`, `PinsStatus`, `PinsEmpty`, `PinsBroken`, `PinChip` (каждая плашка, `AutomationProperties.Name` = название), `PinGlyph` (значок плашки), `PinLabel`, `PinValue`, `PinSave`, `PinCancel`, `PinDelete`; заголовки окна правки — «Новый элемент» и «Изменить».

- [ ] **Step 1: Временный хук разворота** — после `UpdateVisual();` в конструкторе `DockWindow`:

```csharp
        if (Environment.GetEnvironmentVariable("EDGEDOCK_TEST_EXPAND") == "1") { var experiment = new DispatcherTimer { Interval = TimeSpan.FromSeconds(1) }; experiment.Tick += (_, _) => Expand(); experiment.Start(); } // EXPERIMENT
```

(раз в секунду разворачивает панель, если она свернулась от поддельных сообщений мыши в тесте; `Expand()` сам выходит, если уже развёрнуто).

- [ ] **Step 2: Написать тест** `pinstest.ps1`:

```powershell
# Сквозной тест блока «Закреплённое» на тестовом экземпляре EdgeDock (сборка из bin\ с хуком EDGEDOCK_TEST_EXPAND).
# На время теста останавливает установленный виджет; в конце возвращает буфер обмена, pins.json и виджет.
param([string]$Exe = 'D:\Разработочки\Бокоой плагин\EdgeDock\bin\Release\net10.0-windows10.0.19041.0\EdgeDock.exe')
Add-Type -AssemblyName UIAutomationClient, UIAutomationTypes, System.Windows.Forms
if (-not ('T.U' -as [type])) {
Add-Type -Namespace T -Name U -MemberDefinition @'
[DllImport("user32.dll")] public static extern bool SetProcessDpiAwarenessContext(System.IntPtr v);
[DllImport("user32.dll")] public static extern System.IntPtr SendMessage(System.IntPtr h, uint msg, System.IntPtr w, System.IntPtr l);
[StructLayout(LayoutKind.Sequential)] public struct POINT { public int X, Y; }
[DllImport("user32.dll")] public static extern bool ScreenToClient(System.IntPtr h, ref POINT p);
'@
}
[T.U]::SetProcessDpiAwarenessContext([IntPtr](-4)) | Out-Null

$A = [System.Windows.Automation.AutomationElement]
$TS = [System.Windows.Automation.TreeScope]
$pins = Join-Path $env:LOCALAPPDATA 'EdgeDock\pins.json'
$installed = Join-Path $env:LOCALAPPDATA 'Programs\EdgeDock\EdgeDock.exe'
$script:proc = $null
$results = New-Object System.Collections.ArrayList

function Cond($prop, $value) { New-Object System.Windows.Automation.PropertyCondition ($prop, $value) }
function Windows { if (-not $script:proc) { return @() }; @($A::RootElement.FindAll($TS::Children, (Cond $A::ProcessIdProperty $script:proc.Id))) }
function FindId([string]$id) { foreach ($w in Windows) { $e = $w.FindFirst($TS::Descendants, (Cond $A::AutomationIdProperty $id)); if ($e) { return $e } }; $null }
function FindAllId([string]$id) { $all = @(); foreach ($w in Windows) { $all += @($w.FindAll($TS::Descendants, (Cond $A::AutomationIdProperty $id))) }; $all }
function WaitFor([scriptblock]$probe, [int]$ms = 8000) { $end = (Get-Date).AddMilliseconds($ms); do { $r = & $probe; if ($r) { return $r }; Start-Sleep -Milliseconds 150 } while ((Get-Date) -lt $end); $null }
function Check([string]$name, [bool]$ok, $info) { [void]$results.Add(('{0} {1}{2}' -f $(if ($ok) { 'PASS' } else { 'FAIL' }), $name, $(if ($null -ne $info) { "  [$info]" } else { '' }))) }
function Invoke($e) { $e.GetCurrentPattern([System.Windows.Automation.InvokePattern]::Pattern).Invoke() }
function SetText($e, [string]$t) { $e.GetCurrentPattern([System.Windows.Automation.ValuePattern]::Pattern).SetValue($t) }
function GetText($e) { $e.GetCurrentPattern([System.Windows.Automation.ValuePattern]::Pattern).Current.Value }
function Editor { foreach ($w in Windows) { if ($w.Current.Name -in 'Новый элемент', 'Изменить') { return $w } }; $null }
function Chips { FindAllId 'PinChip' }
# Панель могла на миг свернуться (хук разворачивает её раз в секунду) — плашки ждём, а не ищем один раз.
function ChipNames { [void](WaitFor { @(Chips).Count -gt 0 } 5000); (Chips | ForEach-Object { $_.Current.Name }) -join '|' }
function Chip([string]$name) { WaitFor { Chips | Where-Object { $_.Current.Name -eq $name } | Select-Object -First 1 } 5000 }
function Glyph($chip) { $g = $chip.FindFirst($TS::Descendants, (Cond $A::AutomationIdProperty 'PinGlyph')); if ($g -and $g.Current.Name) { [int][char]$g.Current.Name } else { 0 } }
function Status { $s = FindId 'PinsStatus'; if ($s) { $s.Current.Name } else { '' } }

# Правый клик без движения мыши: сообщения прямо окну панели. WPF принимает такой клик, только если
# настоящий курсор стоит над панелью (иначе сверяется с ним и клик теряется) — перед тестом держать курсор над панелью.
function RightClick($e) {
    $r = $e.Current.BoundingRectangle
    $walker = [System.Windows.Automation.TreeWalker]::ControlViewWalker
    $top = $e; while ($top.Current.NativeWindowHandle -eq 0) { $top = $walker.GetParent($top) }
    $h = [IntPtr]$top.Current.NativeWindowHandle
    $p = New-Object T.U+POINT; $p.X = [int]($r.X + $r.Width / 2); $p.Y = [int]($r.Y + $r.Height / 2)
    [T.U]::ScreenToClient($h, [ref]$p) | Out-Null
    $l = [IntPtr](($p.Y -shl 16) -bor ($p.X -band 0xFFFF))
    [T.U]::SendMessage($h, 0x200, [IntPtr]0, $l) | Out-Null   # WM_MOUSEMOVE
    [T.U]::SendMessage($h, 0x204, [IntPtr]2, $l) | Out-Null   # WM_RBUTTONDOWN
    [T.U]::SendMessage($h, 0x205, [IntPtr]0, $l) | Out-Null   # WM_RBUTTONUP
}

function StopWidget {
    $running = @(Get-Process EdgeDock -ErrorAction SilentlyContinue); if (-not $running) { return }
    try { $ev = [System.Threading.EventWaitHandle]::OpenExisting('Local\EdgeDock.Exit'); [void]$ev.Set(); $ev.Dispose() } catch { }
    foreach ($p in $running) { if (-not $p.WaitForExit(10000)) { $p.Kill() } }
}
function StartTest {
    $env:EDGEDOCK_TEST_EXPAND = '1'
    $script:proc = Start-Process $Exe -PassThru
    Remove-Item Env:EDGEDOCK_TEST_EXPAND
    [void](WaitFor { FindId 'PinsAddButton' } 15000)
}
function AddPin([string]$label, [string]$value) {
    Invoke (FindId 'PinsAddButton'); [void](WaitFor { Editor })
    SetText (FindId 'PinLabel') $label; SetText (FindId 'PinValue') $value
    Invoke (FindId 'PinSave'); [void](WaitFor { -not (Editor) }); [void](WaitFor { Chip $label })
}

# Буфер обмена и pins.json пользователя — сохранить (в память и в копию), в конце вернуть.
# Буфер — копией всех форматов (текст, файлы, картинка…), возвращается, только если тест его менял.
$clipSnapshot = New-Object System.Windows.Forms.DataObject
$clipHadData = $false
$clipTouched = $false
$current = [System.Windows.Forms.Clipboard]::GetDataObject()
if ($current) {
    foreach ($format in $current.GetFormats($false)) {
        try { $data = $current.GetData($format, $false); if ($null -ne $data) { $clipSnapshot.SetData($format, $data); $clipHadData = $true } } catch { }
    }
}
$backup = Join-Path $PSScriptRoot 'pins.backup.json'
$hadPins = Test-Path $pins
if ($hadPins) { Copy-Item $pins $backup -Force }

try {
    StopWidget
    if (Test-Path $pins) { Remove-Item $pins }
    StartTest
    if (-not (FindId 'PinsAddButton')) { Check 'pins block present' $false; throw 'no pins block' }
    $empty = FindId 'PinsEmpty'
    Check 'empty hint' ($empty -and -not $empty.Current.IsOffscreen)

    # 1. Добавление
    Invoke (FindId 'PinsAddButton')
    $ed = WaitFor { Editor }
    Check 'plus opens editor' ($ed -and $ed.Current.Name -eq 'Новый элемент')
    Check 'save disabled when empty' (-not (FindId 'PinSave').Current.IsEnabled)
    Invoke (FindId 'PinsAddButton'); Start-Sleep -Milliseconds 500
    Check 'single editor' (@(Windows | Where-Object { $_.Current.Name -in 'Новый элемент', 'Изменить' }).Count -eq 1)
    SetText (FindId 'PinLabel') 'Тест-ссылка'; SetText (FindId 'PinValue') 'https://example.com/edgedock-test'
    Check 'save enabled when filled' ((FindId 'PinSave').Current.IsEnabled)
    Invoke (FindId 'PinSave')
    Check 'editor closes on save' ([bool](WaitFor { -not (Editor) }))
    Check 'chip appears' ([bool](WaitFor { Chip 'Тест-ссылка' }))
    AddPin 'Тест-тег' '#edgedock-test'
    AddPin 'Тест-телефон' '+7 (900) 000-00-00'
    AddPin 'Тест-текст' 'просто текст'
    Check 'order as added' ((ChipNames) -eq 'Тест-ссылка|Тест-тег|Тест-телефон|Тест-текст') (ChipNames)
    $glyphs = (@('Тест-ссылка', 'Тест-тег', 'Тест-телефон', 'Тест-текст') | ForEach-Object { Glyph (Chip $_) }) -join ','
    Check 'icon by value: link, tag, phone, text' ($glyphs -eq '59163,59628,59159,59592') $glyphs
    $saved = Get-Content $pins -Raw -Encoding UTF8 | ConvertFrom-Json   # массив целиком; @(...) в PowerShell 5.1 завернул бы его ещё раз
    Check 'pins.json written' ($saved.Count -eq 4 -and $saved[0].label -eq 'Тест-ссылка' -and $saved[0].value -eq 'https://example.com/edgedock-test')
    $empty = FindId 'PinsEmpty'
    Check 'empty hint hidden' (-not $empty -or $empty.Current.IsOffscreen)

    # 2. Копирование: значение в буфере, галочка на плашке, «Скопировано», соседи не сдвигаются
    $before = (Chip 'Тест-тег').Current.BoundingRectangle
    $clipTouched = $true
    Invoke (Chip 'Тест-ссылка'); Start-Sleep -Milliseconds 300
    Check 'click copies value' ([System.Windows.Forms.Clipboard]::GetText() -eq 'https://example.com/edgedock-test')
    Check 'status Скопировано' ((Status) -eq 'Скопировано') (Status)
    Check 'check mark on chip' ((Glyph (Chip 'Тест-ссылка')) -eq 59198)
    Check 'neighbours do not move' ((Chip 'Тест-тег').Current.BoundingRectangle -eq $before)
    Start-Sleep -Milliseconds 1800
    Check 'confirmation clears' ((Status) -eq '' -and (Glyph (Chip 'Тест-ссылка')) -eq 59163) (Status)

    # 3. Правка по правому клику
    RightClick (Chip 'Тест-ссылка')
    $ed = WaitFor { Editor }
    Check 'right click opens Изменить' ($ed -and $ed.Current.Name -eq 'Изменить')
    Check 'edit prefilled' ((GetText (FindId 'PinLabel')) -eq 'Тест-ссылка' -and (GetText (FindId 'PinValue')) -eq 'https://example.com/edgedock-test')
    SetText (FindId 'PinLabel') 'Тест-ссылка 2'
    Invoke (FindId 'PinSave'); [void](WaitFor { -not (Editor) }); [void](WaitFor { Chip 'Тест-ссылка 2' })
    Check 'edit renames in place' ((ChipNames) -eq 'Тест-ссылка 2|Тест-тег|Тест-телефон|Тест-текст') (ChipNames)

    # 4. Удаление
    RightClick (Chip 'Тест-тег'); [void](WaitFor { Editor })
    Invoke (FindId 'PinDelete'); [void](WaitFor { -not (Editor) }); Start-Sleep -Milliseconds 300
    Check 'delete removes' ((ChipNames) -eq 'Тест-ссылка 2|Тест-телефон|Тест-текст') (ChipNames)

    # 5. Отмена ничего не меняет
    Invoke (FindId 'PinsAddButton'); [void](WaitFor { Editor })
    SetText (FindId 'PinLabel') 'не сохранять'; SetText (FindId 'PinValue') 'x'
    Invoke (FindId 'PinCancel'); [void](WaitFor { -not (Editor) }); Start-Sleep -Milliseconds 300
    Check 'cancel keeps list' ((ChipNames) -eq 'Тест-ссылка 2|Тест-телефон|Тест-текст') (ChipNames)

    # 6. Перезапуск
    StopWidget; StartTest; [void](WaitFor { Chip 'Тест-текст' })
    Check 'survives restart' ((ChipNames) -eq 'Тест-ссылка 2|Тест-телефон|Тест-текст') (ChipNames)

    # 7. Испорченный pins.json: сообщение, «+» неактивна, файл не тронут
    StopWidget
    [IO.File]::WriteAllText($pins, '{ broken')
    StartTest
    $broken = WaitFor { FindId 'PinsBroken' }
    Check 'broken file message' ($broken -and -not $broken.Current.IsOffscreen)
    Check 'add disabled when broken' (-not (FindId 'PinsAddButton').Current.IsEnabled)
    StopWidget
    Check 'broken file untouched' ([IO.File]::ReadAllText($pins) -eq '{ broken')
}
catch { Check "error: $_" $false }
finally {
    StopWidget
    Remove-Item $pins, "$pins.tmp" -ErrorAction SilentlyContinue
    if ($hadPins) { Copy-Item $backup $pins -Force; Remove-Item $backup }
    if ($clipTouched) {
        if ($clipHadData) { [System.Windows.Forms.Clipboard]::SetDataObject($clipSnapshot, $true) }
        else { [System.Windows.Forms.Clipboard]::Clear() }
    }
    if (Test-Path $installed) { Start-Process $installed }
}
$results
'{0} passed, {1} failed' -f @($results | Where-Object { $_ -like 'PASS*' }).Count, @($results | Where-Object { $_ -like 'FAIL*' }).Count
```

- [ ] **Step 3: Собрать и запустить — тест должен упасть**

Run: `dotnet build EdgeDock/EdgeDock.csproj -c Release -v q -nologo`, затем `powershell -ExecutionPolicy Bypass -File <scratchpad>\pinstest.ps1`
Expected: `FAIL pins block present`, `FAIL error: no pins block`, установленный виджет снова запущен.

---

### Task 2: Общий фон Mica, пути, токены

**Files:**
- Create: `EdgeDock/Core/WindowBackdrop.cs`
- Modify: `EdgeDock/Setup/SetupWindow.xaml.cs` (метод `ApplyBackdrop`)
- Modify: `EdgeDock/Core/AppPaths.cs`
- Modify: `EdgeDock/Themes/Tokens.xaml`

**Interfaces:**
- Produces: `WindowBackdrop.Apply(Window window)`; `AppPaths.PinsFile`; ресурсы `Glyph.Add`, `Glyph.Link`, `Glyph.Tag`, `Glyph.Text`, `Glyph.Check`, `Pin.LabelMaxWidth`, `Margin.Chip`, `Margin.ChipList`, `Editor.Width`.

- [ ] **Step 1: `WindowBackdrop.cs`**

```csharp
using System.Windows;
using System.Windows.Interop;
using System.Windows.Media;

namespace EdgeDock.Core;

/// <summary>Фон Mica, как у окон Windows 11, в тон текущей теме — для обычных окон (установка, правка закреплённого).</summary>
internal static class WindowBackdrop
{
    /// <summary>Вызывать из SourceInitialized. Mica недоступна — сплошной фон из токенов.</summary>
    public static void Apply(Window window)
    {
        var hwnd = new WindowInteropHelper(window).Handle;
        HwndSource.FromHwnd(hwnd).CompositionTarget.BackgroundColor = Colors.Transparent;
        var margins = new Native.MARGINS { Left = -1, Right = -1, Top = -1, Bottom = -1 };
        Native.DwmExtendFrameIntoClientArea(hwnd, ref margins);
        Native.SetDwm(hwnd, Native.DWMWA_USE_IMMERSIVE_DARK_MODE, ThemeService.IsDark ? 1 : 0);
        if (Native.SetDwm(hwnd, Native.DWMWA_SYSTEMBACKDROP_TYPE, Native.DWMSBT_MAINWINDOW) != 0)
            window.SetResourceReference(Window.BackgroundProperty, "Brush.Window.Fallback");
    }
}
```

- [ ] **Step 2: `SetupWindow` пользуется им** — в конструкторе `SourceInitialized += (_, _) => WindowBackdrop.Apply(this);`, метод `ApplyBackdrop` удалить; убрать ставшие лишними `using System.Windows.Interop;` и `using System.Windows.Media;`, если больше не нужны.

- [ ] **Step 3: `AppPaths`** — после `StateFile`:

```csharp
    /// <summary>Закреплённое: то, что ввёл пользователь. Отдельно от state.json, чтобы сброс состояния его не задел.</summary>
    public static string PinsFile { get; } = Path.Combine(Root, "pins.json");
```

- [ ] **Step 4: Токены** — в «Модули» после микшера:

```xml
    <!-- Закреплённое: плашки с переносом строк, окно правки. -->
    <sys:Double x:Key="Pin.LabelMaxWidth">140</sys:Double>
    <Thickness x:Key="Margin.Chip">0,0,4,4</Thickness>
    <!-- Нижний отступ плашек (4) гасится, чтобы низ блока был как у остальных. -->
    <Thickness x:Key="Margin.ChipList">0,8,0,-4</Thickness>
    <sys:Double x:Key="Editor.Width">400</sys:Double>
```

и в глифы:

```xml
    <sys:String x:Key="Glyph.Add">&#xE710;</sys:String>
    <sys:String x:Key="Glyph.Link">&#xE71B;</sys:String>
    <sys:String x:Key="Glyph.Tag">&#xE8EC;</sys:String>
    <sys:String x:Key="Glyph.Text">&#xE8C8;</sys:String>
    <sys:String x:Key="Glyph.Check">&#xE73E;</sys:String>
```

- [ ] **Step 5: Сборка** — `dotnet build EdgeDock/EdgeDock.csproj -c Release -v q -nologo`, ожидается 0 ошибок и 0 предупреждений.

---

### Task 3: Модуль «Закреплённое»

**Files:**
- Create: `EdgeDock/Modules/Pins/PinKind.cs`, `PinStore.cs`, `PinItem.cs`, `PinsModule.cs`, `PinsView.xaml`, `PinsView.xaml.cs`, `PinEditorWindow.xaml`, `PinEditorWindow.xaml.cs`
- Modify: `EdgeDock/DockWindow.xaml.cs` (`CreateModule`, `using`)

**Interfaces:**
- Consumes: `WindowBackdrop.Apply`, `AppPaths.PinsFile`, ресурсы Task 2, UIA-идентификаторы Task 1.
- Produces: `PinsModule` (Id `"pins"`).

- [ ] **Step 1: `PinKind.cs`**

```csharp
namespace EdgeDock.Modules.Pins;

/// <summary>Что лежит под названием — по нему выбирается значок плашки.</summary>
public enum PinKind { Link, Tag, Phone, Text }

internal static class PinKinds
{
    /// <summary>
    /// Ссылка — http://, https:// или www.; тег — # или @ в начале; телефон — только цифры, +, пробелы,
    /// скобки и дефисы, цифр не меньше пяти; всё остальное — текст.
    /// </summary>
    public static PinKind Detect(string value)
    {
        string v = value.Trim();
        if (v.StartsWith("http://", StringComparison.OrdinalIgnoreCase) ||
            v.StartsWith("https://", StringComparison.OrdinalIgnoreCase) ||
            v.StartsWith("www.", StringComparison.OrdinalIgnoreCase))
            return PinKind.Link;
        if (v.StartsWith('#') || v.StartsWith('@')) return PinKind.Tag;
        if (v.All(c => char.IsAsciiDigit(c) || c is '+' or ' ' or '(' or ')' or '-') && v.Count(char.IsAsciiDigit) >= 5)
            return PinKind.Phone;
        return PinKind.Text;
    }
}
```

- [ ] **Step 2: `PinStore.cs`**

```csharp
using System.IO;
using System.Text.Encodings.Web;
using System.Text.Json;
using EdgeDock.Core;

namespace EdgeDock.Modules.Pins;

/// <summary>Элемент в pins.json.</summary>
internal sealed class PinEntry
{
    public string Label { get; set; } = "";
    public string Value { get; set; } = "";
}

/// <summary>
/// pins.json — то, что закрепил пользователь. Пишет только виджет, через временный файл.
/// Файл не читается — остаётся как есть, а запись отключается, чтобы не стереть данные.
/// </summary>
internal sealed class PinStore
{
    private static readonly JsonSerializerOptions Json = new()
    {
        PropertyNamingPolicy = JsonNamingPolicy.CamelCase,
        PropertyNameCaseInsensitive = true,
        ReadCommentHandling = JsonCommentHandling.Skip,
        AllowTrailingCommas = true,
        WriteIndented = true,
        Encoder = JavaScriptEncoder.UnsafeRelaxedJsonEscaping, // кириллица в файле остаётся читаемой
    };

    private readonly string _path;

    public PinStore(string path) => _path = path;

    /// <summary>Файл прочитан (или его ещё нет) — можно сохранять.</summary>
    public bool IsReadable { get; private set; } = true;

    public List<PinEntry> Load()
    {
        try
        {
            if (!File.Exists(_path)) return [];
            var entries = JsonSerializer.Deserialize<List<PinEntry>>(File.ReadAllText(_path), Json) ?? [];
            return entries.Where(e => e != null && !string.IsNullOrWhiteSpace(e.Label) && !string.IsNullOrWhiteSpace(e.Value)).ToList();
        }
        catch (Exception ex)
        {
            Log.Error("pins.json: не удалось прочитать, файл оставлен как есть.", ex);
            IsReadable = false;
            return [];
        }
    }

    /// <summary>Сохранить. false — не получилось, или файл не читался и трогать его нельзя.</summary>
    public bool Save(IEnumerable<PinEntry> entries)
    {
        if (!IsReadable) return false;
        try
        {
            Directory.CreateDirectory(Path.GetDirectoryName(_path)!);
            string temp = _path + ".tmp";
            File.WriteAllText(temp, JsonSerializer.Serialize(entries.ToList(), Json));
            File.Move(temp, _path, overwrite: true);
            return true;
        }
        catch (Exception ex)
        {
            Log.Error("pins.json: не удалось сохранить.", ex);
            return false;
        }
    }
}
```

- [ ] **Step 3: `PinItem.cs`**

```csharp
using System.ComponentModel;

namespace EdgeDock.Modules.Pins;

/// <summary>Плашка: название, скрытое под ним значение (видно в подсказке), вид значения и отметка «скопировано».</summary>
public sealed class PinItem : INotifyPropertyChanged
{
    private bool _isCopied;

    public PinItem(string label, string value)
    {
        Label = label;
        Value = value;
        Kind = PinKinds.Detect(value);
    }

    public string Label { get; }
    public string Value { get; }
    public PinKind Kind { get; }

    /// <summary>Только что скопировано — на плашке галочка вместо значка.</summary>
    public bool IsCopied
    {
        get => _isCopied;
        set
        {
            if (_isCopied == value) return;
            _isCopied = value;
            PropertyChanged?.Invoke(this, new(nameof(IsCopied)));
        }
    }

    internal PinEntry ToEntry() => new() { Label = Label, Value = Value };

    public event PropertyChangedEventHandler? PropertyChanged;
}
```

- [ ] **Step 4: `PinsModule.cs`**

```csharp
using System.Collections.ObjectModel;
using System.ComponentModel;
using System.Runtime.InteropServices;
using System.Windows;
using System.Windows.Threading;
using EdgeDock.Core;

namespace EdgeDock.Modules.Pins;

/// <summary>Закреплённое: ссылки, теги, телефоны под названиями; клик копирует. Id в settings.json — "pins".</summary>
internal sealed class PinsModule : IDockModule, INotifyPropertyChanged, IDisposable
{
    private readonly PinStore _store = new(AppPaths.PinsFile);
    private readonly DispatcherTimer _confirmTimer = new() { Interval = TimeSpan.FromSeconds(1.5) };
    private PinItem? _copied;
    private PinEditorWindow? _editor;
    private bool _saveFailed;
    private string _status = "";

    public PinsModule()
    {
        foreach (var entry in _store.Load()) Items.Add(new PinItem(entry.Label, entry.Value));
        _confirmTimer.Tick += (_, _) => EndConfirmation();
    }

    public ObservableCollection<PinItem> Items { get; } = [];

    /// <summary>pins.json не читается: блок показывает ошибку, добавлять нельзя.</summary>
    public bool IsBroken => !_store.IsReadable;

    /// <summary>Надпись в заголовке блока: «Скопировано», «Не удалось скопировать», «Не удалось сохранить» или пусто.</summary>
    public string Status
    {
        get => _status;
        private set
        {
            if (_status == value) return;
            _status = value;
            PropertyChanged?.Invoke(this, new(nameof(Status)));
        }
    }

    public string Id => "pins";

    public bool HasAttention => false;

    public event EventHandler? AttentionChanged
    {
        add { }
        remove { }
    }

    public event PropertyChangedEventHandler? PropertyChanged;

    public FrameworkElement CreateView() => new PinsView(this);

    public void OnExpanded() { }

    public void OnCollapsed() => EndConfirmation();

    public void Suspend() => EndConfirmation();

    public void Resume() { }

    /// <summary>Скопировать значение; на 1,5 с — галочка на плашке и «Скопировано» в заголовке.</summary>
    public void Copy(PinItem item)
    {
        EndConfirmation();
        try
        {
            Clipboard.SetText(item.Value);
            _copied = item;
            item.IsCopied = true;
            Status = "Скопировано";
        }
        catch (ExternalException ex) // буфер занят другой программой, WPF уже повторил попытки
        {
            Log.Error("Закреплённое: не удалось скопировать в буфер обмена.", ex);
            Status = "Не удалось скопировать";
        }
        _confirmTimer.Start();
    }

    /// <summary>Окно добавления (item == null) или правки. Окно одно: если уже открыто — показать его.</summary>
    public void OpenEditor(PinItem? item)
    {
        if (IsBroken) return;
        if (_editor != null)
        {
            _editor.Activate();
            return;
        }
        _editor = new PinEditorWindow(this, item);
        _editor.Closed += (_, _) => _editor = null;
        _editor.Show();
        _editor.Activate();
    }

    public void Add(string label, string value)
    {
        Items.Add(new PinItem(label, value));
        Save();
    }

    public void Update(PinItem item, string label, string value)
    {
        int index = Items.IndexOf(item);
        if (index < 0) return; // элемент уже удалили
        Items[index] = new PinItem(label, value);
        Save();
    }

    public void Remove(PinItem item)
    {
        if (Items.Remove(item)) Save();
    }

    private void Save()
    {
        _saveFailed = !_store.Save(Items.Select(i => i.ToEntry()));
        if (!_confirmTimer.IsEnabled) Status = IdleStatus;
    }

    private string IdleStatus => _saveFailed ? "Не удалось сохранить" : "";

    private void EndConfirmation()
    {
        _confirmTimer.Stop();
        if (_copied != null)
        {
            _copied.IsCopied = false;
            _copied = null;
        }
        Status = IdleStatus;
    }

    public void Dispose()
    {
        EndConfirmation();
        _editor?.Close();
    }
}
```

- [ ] **Step 5: `PinsView.xaml`**

```xml
<!--
  Закреплённое: плашки «значок + название», клик копирует то, что скрыто под названием.
  «+» — добавить, правый клик по плашке — изменить или удалить (в отдельном окне: панель фокус не забирает).
-->
<UserControl x:Class="EdgeDock.Modules.Pins.PinsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:EdgeDock.Modules.Pins"
             Focusable="False">

    <Border Style="{StaticResource Card}">
        <StackPanel>
            <!-- Заголовок: высота задана кнопкой «+», поэтому «Скопировано» ничего не сдвигает. -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Закреплённое" Style="{StaticResource Caption}" VerticalAlignment="Center" />
                <TextBlock Grid.Column="1" Text="{Binding Status}" Style="{StaticResource Caption}"
                           VerticalAlignment="Center" Margin="{StaticResource Margin.Right.S}"
                           AutomationProperties.AutomationId="PinsStatus" />
                <ui:Button Grid.Column="2" Content="{StaticResource Glyph.Add}" ToolTip="Добавить ссылку или тег"
                           Click="OnAdd" AutomationProperties.AutomationId="PinsAddButton">
                    <ui:Button.Style>
                        <Style TargetType="ui:Button" BasedOn="{StaticResource IconButton}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsBroken}" Value="True">
                                    <Setter Property="IsEnabled" Value="False" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ui:Button.Style>
                </ui:Button>
            </Grid>

            <TextBlock Text="Нажмите +, чтобы добавить ссылку или тег" Foreground="{DynamicResource Brush.Text.Tertiary}"
                       Margin="{StaticResource Margin.Top.S}" TextWrapping="Wrap" AutomationProperties.AutomationId="PinsEmpty">
                <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource Caption}">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding Items.Count}" Value="0" />
                                    <Condition Binding="{Binding IsBroken}" Value="False" />
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible" />
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <TextBlock Text="Не удалось прочитать pins.json" Foreground="{DynamicResource Brush.Text.Critical}"
                       Margin="{StaticResource Margin.Top.S}" TextWrapping="Wrap" AutomationProperties.AutomationId="PinsBroken">
                <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource Caption}">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsBroken}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <ItemsControl ItemsSource="{Binding Items}" Focusable="False" Margin="{StaticResource Margin.ChipList}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type local:PinItem}">
                        <ui:Button Style="{StaticResource ChipButton}" Margin="{StaticResource Margin.Chip}"
                                   ToolTip="{Binding Value}" Click="OnPinClick" MouseRightButtonUp="OnPinRightClick"
                                   AutomationProperties.AutomationId="PinChip" AutomationProperties.Name="{Binding Label}">
                            <StackPanel Orientation="Horizontal">
                                <!-- Значки шрифта одной ширины: галочка вместо значка не меняет ширину плашки. -->
                                <TextBlock x:Name="Glyph" Text="{StaticResource Glyph.Text}" FontFamily="{StaticResource Font.Icons}"
                                           FontSize="{StaticResource FontSize.Icon}" Margin="{StaticResource Margin.Right.XS}"
                                           VerticalAlignment="Center" AutomationProperties.AutomationId="PinGlyph" />
                                <TextBlock Text="{Binding Label}" MaxWidth="{StaticResource Pin.LabelMaxWidth}"
                                           TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                            </StackPanel>
                        </ui:Button>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding Kind}" Value="Link">
                                <Setter TargetName="Glyph" Property="Text" Value="{StaticResource Glyph.Link}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Kind}" Value="Tag">
                                <Setter TargetName="Glyph" Property="Text" Value="{StaticResource Glyph.Tag}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Kind}" Value="Phone">
                                <Setter TargetName="Glyph" Property="Text" Value="{StaticResource Glyph.Phone}" />
                            </DataTrigger>
                            <!-- Последним: перекрывает значок вида. -->
                            <DataTrigger Binding="{Binding IsCopied}" Value="True">
                                <Setter TargetName="Glyph" Property="Text" Value="{StaticResource Glyph.Check}" />
                                <Setter TargetName="Glyph" Property="Foreground" Value="{DynamicResource Brush.Accent}" />
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</UserControl>
```

- [ ] **Step 6: `PinsView.xaml.cs`**

```csharp
using System.Windows;
using System.Windows.Controls;
using System.Windows.Input;

namespace EdgeDock.Modules.Pins;

public partial class PinsView : UserControl
{
    private readonly PinsModule _module;

    internal PinsView(PinsModule module)
    {
        _module = module;
        InitializeComponent();
        DataContext = module;
    }

    private void OnAdd(object sender, RoutedEventArgs e) => _module.OpenEditor(null);

    private void OnPinClick(object sender, RoutedEventArgs e)
    {
        if (((FrameworkElement)sender).DataContext is PinItem item) _module.Copy(item);
    }

    private void OnPinRightClick(object sender, MouseButtonEventArgs e)
    {
        if (((FrameworkElement)sender).DataContext is not PinItem item) return;
        e.Handled = true;
        _module.OpenEditor(item);
    }
}
```

- [ ] **Step 7: `PinEditorWindow.xaml`**

```xml
<!--
  Добавление и правка закреплённого. Обычное окно Windows 11 (фон Mica включает код): в нём можно печатать,
  а панель дока фокус не забирает. Enter — сохранить, Esc — закрыть.
-->
<Window x:Class="EdgeDock.Modules.Pins.PinEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Новый элемент"
        Width="{StaticResource Editor.Width}"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        Background="Transparent"
        FontFamily="{StaticResource Font.Text}"
        FontSize="{StaticResource FontSize.Body}"
        Foreground="{DynamicResource Brush.Text.Primary}"
        TextOptions.TextFormattingMode="Display">

    <StackPanel Margin="{StaticResource Inset.Setup}">
        <TextBlock Text="Название" />
        <TextBox x:Name="LabelBox" Margin="{StaticResource Margin.Top.S}" MaxLength="60"
                 TextChanged="OnTextChanged" AutomationProperties.AutomationId="PinLabel" />

        <TextBlock Text="Что копировать" Margin="{StaticResource Margin.Top.M}" />
        <TextBox x:Name="ValueBox" Margin="{StaticResource Margin.Top.S}" MaxLength="2000"
                 TextChanged="OnTextChanged" AutomationProperties.AutomationId="PinValue" />
        <TextBlock Style="{StaticResource Caption}" Margin="{StaticResource Margin.Top.S}" TextWrapping="Wrap"
                   Text="Ссылка, тег, телефон — что угодно. Нажатие на название в панели скопирует это." />

        <Grid Margin="{StaticResource Margin.Top.L}">
            <ui:Button x:Name="DeleteButton" Content="Удалить" HorizontalAlignment="Left" Visibility="Collapsed"
                       Click="OnDelete" AutomationProperties.AutomationId="PinDelete" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="Отмена" IsCancel="True" Click="OnCancel" AutomationProperties.AutomationId="PinCancel" />
                <ui:Button x:Name="SaveButton" Content="Сохранить" Appearance="Primary" IsDefault="True" IsEnabled="False"
                           Margin="{StaticResource Margin.Left.S}" Click="OnSave" AutomationProperties.AutomationId="PinSave" />
            </StackPanel>
        </Grid>
    </StackPanel>
</Window>
```

- [ ] **Step 8: `PinEditorWindow.xaml.cs`**

```csharp
using System.Windows;
using System.Windows.Controls;
using EdgeDock.Core;

namespace EdgeDock.Modules.Pins;

/// <summary>Окно «Новый элемент» / «Изменить»: название и то, что копировать.</summary>
public partial class PinEditorWindow : Window
{
    private readonly PinsModule _module;
    private readonly PinItem? _item;

    internal PinEditorWindow(PinsModule module, PinItem? item)
    {
        _module = module;
        _item = item;
        InitializeComponent();
        if (item != null)
        {
            Title = "Изменить";
            LabelBox.Text = item.Label;
            ValueBox.Text = item.Value;
            DeleteButton.Visibility = Visibility.Visible;
        }
        SourceInitialized += (_, _) => WindowBackdrop.Apply(this);
        Loaded += (_, _) =>
        {
            LabelBox.Focus();
            LabelBox.SelectAll();
        };
    }

    private void OnTextChanged(object sender, TextChangedEventArgs e) =>
        SaveButton.IsEnabled = LabelBox.Text.Trim().Length > 0 && ValueBox.Text.Trim().Length > 0;

    private void OnSave(object sender, RoutedEventArgs e)
    {
        string label = LabelBox.Text.Trim(), value = ValueBox.Text.Trim();
        if (label.Length == 0 || value.Length == 0) return;
        if (_item == null) _module.Add(label, value);
        else _module.Update(_item, label, value);
        Close();
    }

    private void OnDelete(object sender, RoutedEventArgs e)
    {
        if (_item != null) _module.Remove(_item);
        Close();
    }

    private void OnCancel(object sender, RoutedEventArgs e) => Close();
}
```

Внимание: `OnTextChanged` вызывается при установке `LabelBox.Text` в конструкторе — к этому моменту `SaveButton` уже создан (`InitializeComponent` отработал), так что для режима правки «Сохранить» сразу активна.

- [ ] **Step 9: Регистрация** — в `DockWindow.CreateModule` строка `"pins" => new PinsModule(),` после `"audio"`, и `using EdgeDock.Modules.Pins;`.

- [ ] **Step 10: Сборка и тест**

Run: `dotnet build EdgeDock/EdgeDock.csproj -c Release -v q -nologo`, затем `powershell -ExecutionPolicy Bypass -File <scratchpad>\pinstest.ps1`
Expected: все строки `PASS`, `N passed, 0 failed`; установленный виджет снова запущен; `pins.json` пользователя не изменился (его не было — его нет).

- [ ] **Step 11: Снимки** — во время отдельного запуска тестового экземпляра с парой элементов сделать снимок панели (`shotpanel.ps1`) и окна правки; проверить светлую/тёмную тему глазами. Тестовые элементы после этого удалить (файл `pins.json` стереть, если его не было до теста).

- [ ] **Step 12: Коммит** (без строки `// EXPERIMENT`):

```bash
git add EdgeDock SPEC.md docs
git commit -F <scratchpad>/commit-msg.txt   # «Блок «Закреплённое»: ссылки и теги под названиями, клик копирует»
```

---

### Task 4: Документация, версия, выпуск

**Files:**
- Modify: `README.md` («Что умеет», «В планах», «Удаление» — `pins.json` в папке данных уже упомянут общей фразой), `IDEAS.md`, `EdgeDock/EdgeDock.csproj` (`<Version>1.4.0</Version>`)

- [ ] **Step 1: README** — в «Что умеет» после «Звук»:

```markdown
- **Закреплённое.** Ссылки, теги, телефоны под короткими названиями: нажали на название — значение
  в буфере обмена. «+» добавляет, правый клик по названию — изменить или удалить.
```

и в «В планах» убрать «закреплённые телефоны, теги и ссылки».

- [ ] **Step 2: IDEAS.md** — строка: `- Закреплённое: менять порядок плашек перетаскиванием (сейчас — в порядке добавления).`

- [ ] **Step 3: Версия 1.4.0**, убрать строку `// EXPERIMENT`, собрать Release без ошибок, коммит.

- [ ] **Step 4: Выпуск** — `git push origin main`, тег `v1.4.0` и `git push origin v1.4.0`; собрать `dotnet publish EdgeDock/EdgeDock.csproj -c Release -r win-x64 -o artifacts`, установить через `installreal.ps1`; проверить, что установлен и запущен 1.4.0, память ~35 МБ собственная, лог пустой; дождаться зелёной сборки релиза на GitHub.
